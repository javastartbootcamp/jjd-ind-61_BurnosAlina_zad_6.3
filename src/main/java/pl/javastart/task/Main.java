package pl.javastart.task;

public class Main {

    public static void main(String[] args) {
        ArrayUtils utils = new ArrayUtils();
        utils.getRowAndColumns();
        utils.createArray();
        utils.printArray(utils.getArray());
    }
}
